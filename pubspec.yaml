name: neigry
description: НЕИГРЫ — Android phone controller and Android TV stage client.
publish_to: none
version: 0.2.0+200

environment:
  sdk: '>=3.12.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_secure_storage: 11.2.0
  just_audio: 0.10.6
  video_player: 2.14.0
  wakelock_plus: 1.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: 6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/brand/tv_banner.png
